package com.all{		import flash.display.Sprite;	import flash.display.Graphics;	import flash.events.MouseEvent;			public class LineGraph extends Sprite {				//private var _lists:selectedNodesList;		private var _lists:NodeList;		//100,225,350,480		//place holder		private var holder:Array;		private var circelsize:int;		public function LineGraph(lists:NodeList) {			_lists = lists;			circlesize = 10;			holder = new Array();			holder.add(100);			holder.add(225);			holder.add(350);			holder.add(480);		}				public function drawBudgetGraph(){			if(this._lists.length!=0){				this.graphics.lineStyle(2);				//***********compression needed to fit into the display***********//				this.graphics.beginFill(0x66CC99); 						this.graphics.moveTo(holder[0],_list);				this.graphics.lineTo(holder[1],_lists[1].cost);				this.graphics.drawCircle(holder[0],_lists[0].cost,circlesize); 				this.graphics.beginFill(0x330033); 				this.graphics.moveTo(holder[1],_lists[1].cost);				this.graphics.lineTo(holder[2],_lists[2].cost);				this.graphics.drawCircle(holder[1],_lists[1].cost,circlesize);				this.graphics.beginFill(0xFFFF33); 						this.graphics.moveTo(holder[2],_lists[2].cost);				this.graphics.lineTo(holder[3],_lists[3].cost);				this.graphics.drawCircle(holder[2],_lists[2].cost,circlesize);				this.graphics.beginFill(0x3399FF); 				this.graphics.drawCircle(holder[3],_lists[3].cost,circlesize);						}		}		public function update(node:Node){			//TODO : search for the past year data of the selected node			//then 			// update this._lists with the new list of the past 4 years of data then			this.drawBudgetGraph();		}	}	}