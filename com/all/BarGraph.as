package com.all {		import flash.display.Sprite;	import flash.display.Graphics;		public class BarGraph extends Sprite {		private var _lists:NodeList;			private var GraphStageWidth = 100;		private var barwidth = 10;		private var barheight = 10;		private var barspace = 10;		private var xpos = 10;	    private var ypos = 10;				public function BarGraph(lists:NodeList) {			_lists = lists;		}		public function drawBudgetGraph(){			//Todo : make sure it draws the entire selected nodes on the display			if(this._lists.length!=0){				//vertical axis				//				this.graphics.lineStyle(3);				this.graphics.moveTo(xpos, 50);				this.graphics.lineTo(xpos,350);								//horizontal axis				this.graphics.lineStyle(3);				this.graphics.moveTo(xpos,350);				this.graphics.lineTo(500,350);				this.graphics.beginFill(0xFF0000); 								//***********compression needed to fit into the display***********//				var i:int; 				for(i = 0;i<this._lists.length;i++{					this.graphics.drawRect((xpos+i*barwidth+(i+1)*barspace),barheight-datas, barwidth,_lists[i].cost); 					this.graphics.endFill();				}			}		}				public function update(){			//recalculate bar width , bar space etc from the number of the selected nodes								}	}	}